import 'package:flutter/material.dart';

class EgresosPage extends StatelessWidget {
  const EgresosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('egresos'),
      ),
    );
  }
}